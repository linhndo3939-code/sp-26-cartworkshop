# AI Usage Log — M4: Shopping Cart

## Session: Week 8 Lab (March 6, 2026)

### AGENTS.md Setup
- **What:** Created AGENTS.md with project conventions.
- **Result:** Helped the AI generate code matching my folder structure.

### Cart Reducer + Types
- **Generated by:** Gemini
- **Review issues found:** Implicit 'any' types on .map and .filter items.
- **Monday concepts verified:** Discriminated unions, pure reducer, immutable updates.

### Cart Components & Context
- **Issues found:** Folder naming conflicts (CartBadge.css), duplicate imports in App.tsx, and missing 'payload' nesting in dispatch calls.

#### Create src/types/cart.ts with a CartItem type, CartState type, and a CartAction discriminated union (ADD_TO_CART, REMOVE_FROM_CART, UPDATE_QUANTITY, CLEAR_CART, TOGGLE_CART). Next, Create src/reducers/cartReducer.ts with an initialCartState and a cartReader function. ADD_TO_CART should increment quantity if the product exists. Updates must be immutable. Then, Create src/contexts/CartContext.tsx that exports a CartProvider and a custom useCartContext hook. Include derived values: cartItemCount and cartTotal.

#### /explain Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.


#### In CartContext.tsx, I am getting a Fast Refresh error: 'Fast refresh only works when a file only exports components.'

#### Please refactor my code by:

#### Moving the useCartContext hook into its own new file: src/hooks/useCartContext.ts.

#### Keeping CartProvider and CartContext in src/contexts/CartContext.tsx.

#### Ensuring CartContext is exported so the new hook can access it.

#### Fixing the CartContextValue type so it matches the object returned in useMemo (explicitly listing items, isOpen, dispatch, cartItemCount, and cartTotal).

#### In CartContext.tsx, I have red lines on CartContext. Please fix the code with these specific requirements:

#### Add CartState to the type imports from ../types/cart.

#### Explicitly type the useReducer hook so TypeScript knows the state and action types: useReducer<React.Reducer<CartState, CartAction>>(cartReducer, initialCartState).

#### Ensure the import path for cartReducer matches my file name exactly (../reducers/cartReducer).

